diff --git a/crates/token_proxy_cli/src/main.rs b/crates/token_proxy_cli/src/main.rs
index 43e8937..456303e 100644
--- a/crates/token_proxy_cli/src/main.rs
+++ b/crates/token_proxy_cli/src/main.rs
@@ -118,0 +119,5 @@ async fn serve(paths: token_proxy_account_store::paths::TokenProxyPaths) -> Resu
+    // Initialize the proxy's SQLite pools and schemas before the background
+    // pricing refresh opens the same database. Starting both concurrently can
+    // make a fresh headless data directory fail with SQLITE_BUSY.
+    let status = app.start_proxy().await?;
+
@@ -131 +136,0 @@ async fn serve(paths: token_proxy_account_store::paths::TokenProxyPaths) -> Resu
-    let status = app.start_proxy().await?;
