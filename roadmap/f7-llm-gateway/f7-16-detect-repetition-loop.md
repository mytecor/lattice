# Детекция повторных (зацикленных) ответов модели как отдельное routing-правило

Фича: [F7 — LLM gateway](./README.md). Относится к подсистемам typed routing rules
([rule.go](../../packages/llm-gateway/rule.go), [rule_continue.go](../../packages/llm-gateway/rule_continue.go)),
релейному streaming-циклу [server.go](../../packages/llm-gateway/server.go) и
takeover-механизму [router_stream.go](../../packages/llm-gateway/router_stream.go).

**Статус: заведена, не начата.**

## Контекст

Наблюдение на ноде (24.09.2026, сессия pi-acp `hydra_session_x4VnUba7joi3ucZP`) и в прямом
pi без pi-acp: модель `standard` (DeepSeek-V4-Flash-0731 через llm-gateway) входит в состояние
**самоповтора** — начинает бесконечно повторять один и тот же фрагмент вывода. В зацикленной
сессии это выглядело как `Tool call.` ×тысячи с нулевыми реальными tool-вызовами и без
`finish_reason` в течение ~27 минут; контекст сессии при этом вырос с 14k до 61k токенов
(раздутие за счёт повторяемого текста, попадающего в следующий запрос). Проблема воспроизводится
и без pi-acp, т.е. источник — pi-рантайм и/или модель, а не транспорт (`pi-acp`/`acp-normalizer`
только склеивают messageId и не фильтруют повтор).

В llm-gateway уже есть механизм **continue** (`ActionContinue`, rule `continue`) — при обрыве
стрима (нет данных / нет `finish_reason` / stall watchdog) он перезапрашивает с накопленной
историей через `ContinueStream`. Но это **зашитое ядровое поведение**, и оно реагирует только
на обрыв, а не на **содержательный повтор** (модель ответила дважды одинаково и не завершает
turn). Такой стрим идёт клиенту до бесконечности, жжёт output-токены и раздувает контекст.

**Требование (из запроса):** детектор повторов должен быть **отдельным routing-правилом,
которое можно опционально подключать** на нужные логические модели, а не частью ядра
llm-gateway. Это совпадает с архитектурой F7: routing actions живут как отдельные типы в
`ruleRegistry`, валидируются и применяются через `apply`, подключаются на entry-route; ядро
не получает зашитой логики, а только вызывает опциональную политику.

## Что сделать

- [ ] **Новое routing-действие `repetition` (имя уточнить, например `loop_guard`).** Добавить
      в [rule.go](../../packages/llm-gateway/rule.go): константу `ActionRepetition`,
      дескриптор в `ruleRegistry` (рядом с `continue`, rank 13/14), новый файл
      `rule_repetition.go`. Правило **никогда не включается по умолчанию** — отсутствие правила
      на route означает «детекции нет», ровно как у `continue`.
- [ ] **Типизированная конфигурация правила.** По аналогии с `ContinueRule`:
      - `enabled` не нужен — само наличие правила на entry-route включает детекцию;
      - содержимое домена детектора: порог повтора (например `Repeats`/`Window`),
        режим сравнения («порядковый повтор» и/или «накопленный текст последних L символов»),
        стоит ли останавливаться сразу или после K-го повтора;
      - `apply` валидирует (положительный порог, верхний предел окна, осмысленный режим) и
        пишет в `ContinueConfig`/новую структуру политики entry-route. Минимум порога — чтобы
        не срабатывать на естественных повторениях слов в связной речи и на
        документированных «reasoning`only, 0 content»-кейсах (см.
        [continue.go](../../packages/llm-gateway/continue.go) `GotToolCalls`).
- [ ] **Точка вызова в релейном цикле — только через политику, без зашитой логики.**
      В [server.go](../../packages/llm-gateway/server.go) рядом с существующей
      `continuePolicy(logical)` добавить чтение опциональной политики `repetition`.
      Если политики нет — релейный цикл ведёт себя как сегодня (никакой детекции).
      Если есть — в цикл накопления `partial` добавляется проверка повтора как **ещё одно
      условие «стоп → continue»**, наравне с idle/нет-finish_reason. Реализация детектора
      живёт в файле правила/отдельном файле, а не в `server.go`.
- [ ] **Детектор как самостоятельный компонент.** Нормализация (нижний регистр, схлопывание
      пробелов/пунктуации, терминирование на границах токенов «Tool/ call/ .») и правило
      сравнения — в собственном файле (например `repetition.go`), покрыть unit-тестами.
      Не смешивать с релейным циклом.
- [ ] **Стоп + перезапрос с историей через существующий continue.** При срабатывании порога:
      прервать текущий стрим (терминальный SSE-сигнал по существующему error-contract
      [server.go](../../packages/llm-gateway/server.go), без повторной отправки старого
      oversized source) и вызвать уже существующий `ContinueStream` с накопленным `partial`
      (`appendPartialChatHistory`) — тот же путь, что у stall/`no finish_reason`.
- [ ] **Защита от рецидива и bounded.** Повторный продолженный стрим снова проходит детектор;
      при повторном срабатывании порога цепочка continue ограничивается (существующие
      `maxContinueChainRetries`/`Wait`) — не входить в бесконечный цикл continue→повтор→continue.
- [ ] **Метрики/события.** Счётчик `llm_repetition_detected_total{route,provider}` и structured
      event по контрактам [F12](../f12-observability/README.md); инкремент ровно один раз на
      событие. Без sensitive/high-cardinality labels.
- [ ] **Пример подключения и документ.** В [README F7](./README.md) и/или в комментарии правила
      показать, как опционально включить на конкретную логическую модель (например `standard`),
      и что по умолчанию детекция выключена. Политику тестов смотреть
      [tests/README.md](../../tests/README.md).

## Критерий готовности (Definition of Done)

- [ ] На моделях без правила `repetition` поведение релейного цикла не меняется (регрессия
      существующих тестов стрима чистая); на моделях с подключённым правилом стрим с
      K-кратным самоповтором останавливается и уходит в continue (новый запрос с историей)
      вместо бесконечной выдачи.
- [ ] Go-тесты покрывают: детектор (unit, синтетические стримы + естественный неповторяющийся
      текст), правило (валидация, apply в политику, декод через registry), integration
      (повтор → stop + continue; рецидив ограничен; метрика ровно один раз). Проверка на ноде —
      повторное воспроизведение «Tool call.»-зацикленности: контекст перестаёт вздуваться,
      стрим завершается.

## Затрагиваемые файлы / слои

- `packages/llm-gateway/rule.go` — `ActionRepetition` + дескриптор в `ruleRegistry`.
- `packages/llm-gateway/rule_repetition.go` — новый тип правила, `apply` пишет политику.
- `packages/llm-gateway/repetition.go` — детектор (нормализация + сравнение) как чистый компонент.
- `packages/llm-gateway/server.go` — опциональный вызов политики в релейном цикле (никакой
  зашитой логики детекции), ветка «стоп → ContinueStream».
- `packages/llm-gateway/continue.go` / `router_stream.go` — переиспользование
  `partialStreamOutput`/`appendPartialChatHistory`/`ContinueStream` (не переотправлять сырой
  источник).
- `packages/llm-gateway/metrics.go` — `llm_repetition_detected_total`.
- Тесты: `packages/llm-gateway/repetition*_test.go`, расширение `stream_failure_test.go`.

## Открытые вопросы

- **Имя действия:** `repetition`, `loop_guard`, `guard_loop` — выбрать по конвенции F7
  (глагол/существительное одним словом; см. `filter/map/rank/...`).
- **Что считать «повтором»:** порядковый (N-й подряд идентичный нормализованный фрагмент) или
  накопленный текст (последние L символов совпадают с уже выданным)? Порядковый проще и не даёт
  ложных срабатываний; накопленный надёжнее ловит «Tool call.». Возможно оба с опцией.
- **Взаимодействие с `finish_reason`:** нормальный финиш (finish_reason) повтор не останавливает —
  только «не закрывающийся» стрим, продолжающий дублировать.
- **Порог по умолчанию:** безопасный дефолт, при котором детекция не трогает нормальные длинные
  ответы (в т.ч. GLM reasoning-only-кейс из [continue.go](../../packages/llm-gateway/continue.go)).
- **Стоп для клиента:** терминальный SSE `[DONE]` или error — чтобы pi не превращал стоп в
  собственный retry (у pi есть `pi-retry` на `abort`/`upstream stream failed`).
