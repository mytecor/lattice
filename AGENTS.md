# AGENTS.md

Инструкции для агентов, работающих в этом репозитории.

## Базовая документация

- [README.md](./README.md) — назначение проекта и общий указатель документации.
- [ARCHITECTURE.md](./ARCHITECTURE.md) — архитектурные решения и границы компонентов.
- [DEPLOYMENT.md](./DEPLOYMENT.md) — развёртывание и обязательная процедура публикации в Radicle
  и GitHub.
- [CONTRIBUTING.md](./CONTRIBUTING.md) — правила внесения и проверки изменений.

При запросе на push следуйте разделу
[«Публикация в Radicle и GitHub»](./DEPLOYMENT.md#публикация-в-radicle-и-github).

## Документация: оформление ссылок на другие файлы

Все ссылки на другие файлы внутри документации **обязаны** быть оформлены
через Markdown-форматирование относительных ссылок между файлами, а не как
голый текст пути.

Правильно:

```markdown
См. [ARCHITECTURE.md](./ARCHITECTURE.md).
```

Неправильно (`ARCHITECTURE.md` без link-разметки):

```markdown
См. ARCHITECTURE.md.
```

Требования:

- Ссылка оформляется как `[текст](./относительный/путь)` — с префиксом `./`
  для относительного пути от текущего файла.
- Не использовать абсолютные пути, внешние файловые пути или простое
  упоминание имени файла без Markdown-ссылки.
- Ломаные ссылки в Markdown, HTML или на живом сайте проверяются через `lychee`.

### Локальные ссылки не оставляем в документации

Ссылки на локальные сети и внутренние хосты (например `*.local`, `*.lan`, IP из
частных диапазонов, `localhost`) **не оставляем** в документации как голые URL
в тексте: из CI внешнего провайдера они недостижимы, и проверка `lychee`
падает на них (Connection failed). Это не сломанная ссылка, а недопустимая
форма записи.

Если адрес нужен в документации, записывайте его **как код** — в `backticks`
или внутри fenced-блока (` ```text `, ` ```sh ` и т.п.) — тогда `lychee`
пропустит его как литерал.

Правильно:

```markdown
Адрес: `http://grafana.mytecor-homelab.local/`
```

Неправильно (голый URL в прозе — проверяется и падает в CI):

```markdown
Адрес: http://grafana.mytecor-homelab.local/
```

Публичные же ссылки проверяются `lychee` как обычно и должны быть рабочими.

## Tests

Before adding or changing tests, read [tests/README.md](./tests/README.md).

Tests should verify contracts, invariants, generated artifacts, and runtime behavior.

Do not write snapshot-like assertions that merely duplicate values from node configuration. If changing a configuration value would be a legitimate change, a test should normally not fail because of it.

Universal configuration constraints belong in NixOS module `assertions`, not in tests of a specific node.

## Секреты и пароли

Секреты агентов (ключи нод, пароли сервисов, admin-пароли из `*.age`) являются
чувствительными данными и не должны попадать в контекст LLM-моделей.

- Агент **не должен самостоятельно вычитывать** секрет в свой контекст: не запускать
  команды расшифровки (`age --decrypt`, `agenix -r`/`-e`, `cat` секрета и т.п.)
  с выводом в консоль/stdout.
- Вместо этого вывод расшифровки можно **пайпить куда нужно** (в файл, в переменную,
  в stdin другого процесса), не печатая значение в консоль.
- Главное правило: секрет в чистом виде **не должен уйти провайдерам LLM-моделей**
  (не попадать в stdout/tool-вывод, который читает агент как context).

Пример допустимого использования (значение не появляется в консоли):

```sh
# значение идёт сразу в файл, агент его не видит и не печатает
age --decrypt -i "$key" secrets/foo.age > /tmp/foo.secret
# или прямо в stdin следующей команды
age --decrypt -i "$key" secrets/foo.age | some-command
```

Если секрет всё же попал в вывод (например, диагностика проблемы входа), агент
должен продолжать писать команды так, чтобы не повторять секрет в stdout-выводе,
а результат работы фиксировать обезличенно (путь к файлу, статус, код ответа).
