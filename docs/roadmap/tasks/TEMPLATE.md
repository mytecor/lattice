# Шаблон задачи

Скопируйте этот файл в каталог своей фичи как
`docs/roadmap/<feature-id>-<feature-slug>/<feature-id>-<task-id>-<task-slug>.md` и заполните.
`<feature-id>` — номер фичи, `<feature-slug>` — слаг фичи, `<task-id>` — номер задачи,
`<task-slug>` — слаг задачи. Каталог называется по имени файла фичи
(`<feature-id>-<feature-slug>/`). Пример: `f1-05-wireless-on-node.md` в каталоге `../f1-one-node/`.

Затем добавьте ссылку на задачу в файл своей фичи (`README.md` в каталоге
`<feature-id>-<feature-slug>/`) и строку в реестр
[`tasks/README.md`](./README.md), если он поддерживается.

---

# Название задачи (делает X)

<!--
Короткий заголовок: что делаем. Одна мысль, без деталей.
-->

## Контекст

Почему эта задача нужна. Укажите фичу, которую она двигает, и от чего зависит.

## Что сделать

<!--
Чек-лист конкретных шагов. Каждый шаг — это проверяемое действие, а не «сделать хорошо».
-->

- [ ] Шаг 1
- [ ] Шаг 2

## Критерий готовности (Definition of Done)

<!--
Как проверить, что задача закрыта. Один-два измеримых пункта. Если критерий не измерим —
уточните, пока он не станет измеримым.
-->

- [ ] Критерий 1
- [ ] Критерий 2

## Затрагиваемые файлы / слои

<!--
Какие слои (hardware/modules/profiles/nodes) и какие файлы меняются. Для расхождений «код
против документации» укажите оба места.
-->

- `...`

## Открытые вопросы

<!--
Если что-то требует решения до старта — напишите здесь и добавьте в BACKLOG.md. Если пусто —
удалите раздел.
-->

_нет_
