# Шаблон фичи

Скопируйте этот файл в `docs/roadmap/features/<id>-<slug>.md` и заполните. `<id>` — номер вида
`f7` (порядковый номер фичи), `<slug>` — короткое имя. Пример: `f7-wireless-nodes.md`.

Затем добавьте строку фичи в [`features/README.md`](./README.md), ссылку на неё из нужной
[вехи](../VISION.md#вехи-и-зависимости-без-деталей) и заведите задачи в
[`tasks/`](../tasks/README.md) по [TEMPLATE.md](../tasks/TEMPLATE.md).

> Номер фичи — стабильный идентификатор. Порядок реализации задаётся явными зависимостями, а не
> номером или календарём. Новую фичу заводите только для потребности, которой не покрывают уже
> существующие вертикали.

---

# F7. Название фичи (короткая суть)

<!--
Один абзац: что это за вертикаль и зачем она нужна. Без деталей реализации.
-->

Соответствует [вехе N](../VISION.md#вехи-и-зависимости-без-деталей).

Задачи: [f7-01](../tasks/f7-01-<slug>.md),
[f7-02](../tasks/f7-02-<slug>.md).

**Критерий готовности:** <!-- один-два измеримых пункта, как проверить, что фича закрыта -->

**Осознанно откладываем (до F…):** <!-- что сознательно не входит в эту фичу и когда оно появится -->
